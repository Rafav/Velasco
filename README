
python3 reanudable.py
for file in *.json; do   [ -f "$file" ] || continue;   if jq -e '.ocr | fromjson' "$file" > "${file%.json}-OCR.json" 2>/dev/null; then     echo "✓ Procesado: $file";   else     echo "✗ Error: $file";   fi; done
